name: Rust test

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '**.rs'
      - 'Cargo.toml'
      - '**/Cargo.toml'
      - 'Cargo.lock'
      - '**/Cargo.lock'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/rust-ci.yaml@bb7d80df12b3efd7d5b4f0611beaff68176c0747 # main
