name: Rust test

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '**.rs'
      - 'Cargo.toml'
      - '**/Cargo.toml'
      - 'Cargo.lock'
      - '**/Cargo.lock'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/rust-ci.yaml@8b6874f56dc36ecfa44922d192b5e390e602b7f4 # main
