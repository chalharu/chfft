name: Rust test

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '**.rs'
      - 'Cargo.toml'
      - '**/Cargo.toml'
      - 'Cargo.lock'
      - '**/Cargo.lock'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/rust-ci.yaml@7548b46a59ba9d03fd4cdf12e485e36692eac003 # main
